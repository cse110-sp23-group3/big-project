window.addEventListener('DOMContentLoaded', init);

/**
 * The main function that runs on this page
 */
function init() {
  // TODO: create format for JSON file to hold people's info; parse that file and populate people's info into cards
}
